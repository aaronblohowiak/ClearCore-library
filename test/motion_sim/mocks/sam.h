#pragma once
// Mock hardware intrinsics for PC simulation
// These are normally provided by the SAM microcontroller headers

#define __disable_irq() do {} while(0)
#define __enable_irq() do {} while(0)
